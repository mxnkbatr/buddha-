pluginManagement {
    repositories {
        google()
        mavenCentral()
        gradlePluginPortal()
    }
}
plugins {
    id "com.capacitorjs.app.settings" version "7.0.0"
}

include ':app'
rootProject.name = 'Gevabal'
